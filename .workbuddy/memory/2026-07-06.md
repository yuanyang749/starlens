# 2026-07-06

## /init — 核对并补全 CLAUDE.md
- 运行 `/init`，未推翻现有 CLAUDE.md（已相当完整），而是核实现实后做精准补充。
- 核实事实：8 个包名（apps: web/mobile/cli/mcp；packages: core/server/workbench/agent-tools）与 CLAUDE.md 完全一致。
- 新增内容：
  1. 顶层支撑目录地图：`scripts/`（with-env/sync-skill/check-api-shims/deploy/bin）、`drizzle/`、`docs/`、`deploy/`、`agent-skills/starlens`、`discuss/`、`design-assets/`、`marketing-video/`。
  2. `### Environment Files`：`.env`/`.env.example`/`.env.neon.example` 在根目录且被 gitignore；本地复制 .env.example，Neon 复制 .env.neon.example。
  3. `### Database & Migrations (gotcha)`：权威 drizzle 配置是根目录 `drizzle.config.ts`；`db:*` 脚本从 apps/web 运行且 `--config ../../drizzle.config.ts`，故 schema 路径相对于 apps/web 解析；真实 schema 在 `packages/server/src/db/schema.ts`，由 `apps/web/src/db/schema.ts` 再导出；不要手写迁移 SQL。
  4. 强调 `agent-skills/starlens` 是 `stars install-skill` 的唯一源（postinstall 的 `sync-skill.mjs` 同步进 CLI），改 skill 改这里而非副本。
- 未改动原文的命令、架构、数据流、代码约定章节。
